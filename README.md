A persistent mongodb in the lastest version, with a minimal node v16 express app.js using mongoose   

Use:

docker-compose build && docker-compose up


then connect to localhost:4280 to access app.js
